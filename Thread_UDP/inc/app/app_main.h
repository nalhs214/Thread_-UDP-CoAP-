#ifndef APP_MAIN_H
#define APP_MAIN_H

/* ---------------------------------------------------------------------------
 * app_main.h  —  애플리케이션 진입 인터페이스
 *
 *   main() 은 app_init() 하나만 호출한다. 실제 초기화 순서와
 *   모듈 연결(글루)은 app_main.c 가 담당한다.
 * ------------------------------------------------------------------------- */

/* 애플리케이션 초기화:
 *   Work Queue 시작 → LED/버튼/UDP 준비 → FSM 시작(Thread 구동)
 * 반환: 0 성공, 음수 실패 */
int app_init(void);

#endif /* APP_MAIN_H */
